import type { JSX } from 'react';
import { PagePlaceholder } from '../../../components/PagePlaceholder.js';

export function ActivatePage(): JSX.Element {
  return (
    <PagePlaceholder
      title="Activate account"
      description="Invite activation flow arrives with Milestone 3."
    />
  );
}
