// App shell placeholder. ui-agent expands in Milestone 2 with providers, router,
// theme, and navigation. This keeps bootstrap compilable and runnable.
import type { JSX } from 'react';

export function App(): JSX.Element {
  return (
    <main style={{ padding: '2rem', fontFamily: 'system-ui, sans-serif' }}>
      <h1>OrgFlow AI</h1>
      <p>Bootstrap complete. UI shell arriving in Milestone 2.</p>
    </main>
  );
}
